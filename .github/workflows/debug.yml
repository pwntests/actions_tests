name: debug

on:
  push:

jobs:
  enum:
    runs-on: ubuntu-latest
    steps:
      - id: dump context
        run: |
          echo ${{ github.ref }}
          echo ${{ github.ref_name }}
          cat <<"EOF"
          ${{ toJSON(github.event) }}
          EOF
